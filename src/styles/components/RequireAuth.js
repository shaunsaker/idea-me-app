import styleConstants from '../styleConstants'; 

const styles = {
    container: {
        
        justifyContent: 'center',
        alignItems: 'center',
        position: 'relative',
    },
};

export default styles;
